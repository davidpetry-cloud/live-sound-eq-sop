{
  "name": "live-sound-eq-sop",
  "version": "1.0.0",
  "description": "Channel EQ starting points, monitor ring-out guide and frequency ownership map for the Allen & Heath MixWizard WZ3 16:2.",
  "type": "module",
  "author": "David Petry",
  "license": "MIT",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "build": "mkdir -p public/src && cp src/*.js public/src/ && mkdir -p public/vendor/attestation-ledger && cp node_modules/attestation-ledger/src/index.js public/vendor/attestation-ledger/index.js",
    "preview": "npm run build && npx --yes serve public",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage"
  },
  "dependencies": {
    "attestation-ledger": "^0.1.0"
  },
  "devDependencies": {
    "vitest": "^5.0.1"
  },
  "keywords": [
    "live-sound",
    "audio-engineering",
    "foh",
    "mixing-console",
    "allen-and-heath"
  ]
}
